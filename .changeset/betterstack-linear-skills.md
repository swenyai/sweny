---
"@sweny-ai/core": minor
---

Add BetterStack built-in skill (log queries via ClickHouse HTTP + source management via REST API) and enhance Linear skill with get_issue and list_teams tools. Built-in skills provide CI-native tool access in GitHub Actions and cloud environments where MCP servers cannot run.

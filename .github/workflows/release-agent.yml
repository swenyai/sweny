name: Release @sweny/agent

on:
  push:
    tags:
      - "agent-v*"

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          registry-url: https://registry.npmjs.org

      - run: npm ci

      - name: Build providers (dependency)
        run: npm run build --workspace=packages/providers

      - name: Build agent
        run: npm run build --workspace=packages/agent

      - name: Typecheck
        run: npm run typecheck --workspace=packages/agent

      - name: Test
        run: npm run test --workspace=packages/agent

      - name: Publish
        run: npm publish --access public --workspace=packages/agent
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

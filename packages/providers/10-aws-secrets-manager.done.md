# AWS Secrets Manager Credential Vault Provider

Add `awsSecretsManager` provider to `credential-vault/` implementing `CredentialVaultProvider`.

- AWS Secrets Manager SDK
- Config: `region`, `prefix`
- Full read/write/delete/list support

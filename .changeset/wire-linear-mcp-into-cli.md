---
"@sweny-ai/cli": minor
---

Add `linear-mcp` as a selectable issue tracker provider. Set `--issue-tracker-provider linear-mcp` (with `LINEAR_API_KEY`) to use the Linear MCP server adapter instead of the native REST client.

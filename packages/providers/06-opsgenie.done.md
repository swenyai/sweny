# OpsGenie Incident Provider

Add `opsgenie` provider to `incident/` implementing `IncidentProvider`.

- OpsGenie REST API v2
- Config: `apiKey`, `region` (us/eu)
- Create/acknowledge/resolve alerts, query on-call schedules

---
"@sweny-ai/core": minor
---

`sweny new` now offers "Describe your own" as a picker option — AI-generated
workflow creation directly from the unified command. The generated workflow
flows through the same provider-inference and credential-collection logic as
the built-in templates. `sweny workflow create` is deprecated in favor of
`sweny new`.

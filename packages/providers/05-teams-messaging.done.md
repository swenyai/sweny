# Microsoft Teams Messaging Provider

Add `teams` provider to `messaging/` implementing `MessagingProvider`.

- Microsoft Graph API for chat messages
- Config: `tenantId`, `clientId`, `clientSecret` (app registration)
- Send/update messages in Teams channels

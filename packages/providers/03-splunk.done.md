# Splunk Observability Provider

Add `splunk` provider to `observability/` implementing `ObservabilityProvider`.

- Splunk REST API (search/jobs endpoint)
- Config: `baseUrl`, `token`, `index`
- SPL query generation for log search and aggregation

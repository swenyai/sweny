---
"@sweny-ai/studio": minor
---

Proxy AI requests through Vite dev server middleware instead of calling Anthropic API directly from the browser. No API key needed in the browser — authentication is handled server-side via Claude Code. Adds /api/generate-workflow, /api/refine-workflow, and /api/generate-instruction endpoints.

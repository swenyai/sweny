---
"@sweny-ai/engine": minor
---

Add optional `provider` field to `StateDefinition` for declaring which provider category a state uses. Updates the built-in `triageDefinition` and `implementDefinition` with full `provider` annotations and rich `description` text on every state. Pure metadata — no runtime behaviour change.

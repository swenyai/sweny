# Elastic/Kibana Observability Provider

Add `elastic` provider to `observability/` implementing `ObservabilityProvider`.

- Elasticsearch REST API (_search endpoint)
- Config: `baseUrl`, `apiKey` or `username`/`password`, `index`
- Uses Elasticsearch Query DSL

---
"@sweny-ai/providers": minor
---

Add browser-safe `PROVIDER_CATALOG` — a structured list of all available provider implementations with display names, categories, env var specs (key, description, required, secret, example), and import paths. Exported as `PROVIDER_CATALOG`, `getProvidersForCategory()`, and `getProviderById()` from the main package entry.

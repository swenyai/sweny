---
"@sweny-ai/core": minor
---

Add `workflow create` and `workflow edit` CLI commands. `workflow create <description>` generates a new workflow from a natural language description with an interactive save/refine loop. `workflow edit <file> [instruction]` edits an existing workflow file using natural language instructions with the same interactive loop. Both commands support `--json` for non-interactive output.

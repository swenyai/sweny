---
"@sweny-ai/cli": patch
---

CLI now prints actionable hints for credential errors: pre-run validation messages include direct links to get API keys, and runtime failures with auth/network errors display a targeted hint suggesting which credential to check.

---
"@sweny-ai/studio": minor
---

Simulate mode now has an "Auto-run" button that runs all steps as stubs (always success), making it easy to visualize the full execution flow of any workflow — including custom workflows — without manual stepping.
